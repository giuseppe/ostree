/*
 * Generated by gdbus-codegen 2.48.1. DO NOT EDIT.
 *
 * The license of this code is the same as for the source it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "ostree-fetch-service.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface ostree.fetch.service
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:OstreeFetchService
 * @title: OstreeFetchService
 * @short_description: Generated C code for the ostree.fetch.service D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-ostree-fetch-service.top_of_page">ostree.fetch.service</link> D-Bus interface in C.
 */

/* ---- Introspection data for ostree.fetch.service ---- */

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_fetch_object_IN_ARG_objtype =
{
  {
    -1,
    (gchar *) "objtype",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_fetch_object_IN_ARG_checksum =
{
  {
    -1,
    (gchar *) "checksum",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_fetch_object_IN_ARG_max_size =
{
  {
    -1,
    (gchar *) "max_size",
    (gchar *) "t",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _ostree_fetch_service_method_info_fetch_object_IN_ARG_pointers[] =
{
  &_ostree_fetch_service_method_info_fetch_object_IN_ARG_objtype,
  &_ostree_fetch_service_method_info_fetch_object_IN_ARG_checksum,
  &_ostree_fetch_service_method_info_fetch_object_IN_ARG_max_size,
  NULL
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_fetch_object_OUT_ARG_temp_path =
{
  {
    -1,
    (gchar *) "temp_path",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _ostree_fetch_service_method_info_fetch_object_OUT_ARG_pointers[] =
{
  &_ostree_fetch_service_method_info_fetch_object_OUT_ARG_temp_path,
  NULL
};

static const _ExtendedGDBusMethodInfo _ostree_fetch_service_method_info_fetch_object =
{
  {
    -1,
    (gchar *) "fetch_object",
    (GDBusArgInfo **) &_ostree_fetch_service_method_info_fetch_object_IN_ARG_pointers,
    (GDBusArgInfo **) &_ostree_fetch_service_method_info_fetch_object_OUT_ARG_pointers,
    NULL
  },
  "handle-fetch-object",
  FALSE
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_fetch_delta_part_IN_ARG_path =
{
  {
    -1,
    (gchar *) "path",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_fetch_delta_part_IN_ARG_max_size =
{
  {
    -1,
    (gchar *) "max_size",
    (gchar *) "t",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _ostree_fetch_service_method_info_fetch_delta_part_IN_ARG_pointers[] =
{
  &_ostree_fetch_service_method_info_fetch_delta_part_IN_ARG_path,
  &_ostree_fetch_service_method_info_fetch_delta_part_IN_ARG_max_size,
  NULL
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_fetch_delta_part_OUT_ARG_temp_path =
{
  {
    -1,
    (gchar *) "temp_path",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _ostree_fetch_service_method_info_fetch_delta_part_OUT_ARG_pointers[] =
{
  &_ostree_fetch_service_method_info_fetch_delta_part_OUT_ARG_temp_path,
  NULL
};

static const _ExtendedGDBusMethodInfo _ostree_fetch_service_method_info_fetch_delta_part =
{
  {
    -1,
    (gchar *) "fetch_delta_part",
    (GDBusArgInfo **) &_ostree_fetch_service_method_info_fetch_delta_part_IN_ARG_pointers,
    (GDBusArgInfo **) &_ostree_fetch_service_method_info_fetch_delta_part_OUT_ARG_pointers,
    NULL
  },
  "handle-fetch-delta-part",
  FALSE
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_fetch_delta_super_IN_ARG_from_revision =
{
  {
    -1,
    (gchar *) "from_revision",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_fetch_delta_super_IN_ARG_to_revision =
{
  {
    -1,
    (gchar *) "to_revision",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_fetch_delta_super_IN_ARG_branch =
{
  {
    -1,
    (gchar *) "branch",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _ostree_fetch_service_method_info_fetch_delta_super_IN_ARG_pointers[] =
{
  &_ostree_fetch_service_method_info_fetch_delta_super_IN_ARG_from_revision,
  &_ostree_fetch_service_method_info_fetch_delta_super_IN_ARG_to_revision,
  &_ostree_fetch_service_method_info_fetch_delta_super_IN_ARG_branch,
  NULL
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_fetch_delta_super_OUT_ARG_result =
{
  {
    -1,
    (gchar *) "result",
    (gchar *) "ay",
    NULL
  },
  TRUE
};

static const _ExtendedGDBusArgInfo * const _ostree_fetch_service_method_info_fetch_delta_super_OUT_ARG_pointers[] =
{
  &_ostree_fetch_service_method_info_fetch_delta_super_OUT_ARG_result,
  NULL
};

static const _ExtendedGDBusMethodInfo _ostree_fetch_service_method_info_fetch_delta_super =
{
  {
    -1,
    (gchar *) "fetch_delta_super",
    (GDBusArgInfo **) &_ostree_fetch_service_method_info_fetch_delta_super_IN_ARG_pointers,
    (GDBusArgInfo **) &_ostree_fetch_service_method_info_fetch_delta_super_OUT_ARG_pointers,
    NULL
  },
  "handle-fetch-delta-super",
  FALSE
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_fetch_ref_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _ostree_fetch_service_method_info_fetch_ref_IN_ARG_pointers[] =
{
  &_ostree_fetch_service_method_info_fetch_ref_IN_ARG_name,
  NULL
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_fetch_ref_OUT_ARG_result =
{
  {
    -1,
    (gchar *) "result",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _ostree_fetch_service_method_info_fetch_ref_OUT_ARG_pointers[] =
{
  &_ostree_fetch_service_method_info_fetch_ref_OUT_ARG_result,
  NULL
};

static const _ExtendedGDBusMethodInfo _ostree_fetch_service_method_info_fetch_ref =
{
  {
    -1,
    (gchar *) "fetch_ref",
    (GDBusArgInfo **) &_ostree_fetch_service_method_info_fetch_ref_IN_ARG_pointers,
    (GDBusArgInfo **) &_ostree_fetch_service_method_info_fetch_ref_OUT_ARG_pointers,
    NULL
  },
  "handle-fetch-ref",
  FALSE
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_fetch_summary_OUT_ARG_result =
{
  {
    -1,
    (gchar *) "result",
    (gchar *) "ay",
    NULL
  },
  TRUE
};

static const _ExtendedGDBusArgInfo * const _ostree_fetch_service_method_info_fetch_summary_OUT_ARG_pointers[] =
{
  &_ostree_fetch_service_method_info_fetch_summary_OUT_ARG_result,
  NULL
};

static const _ExtendedGDBusMethodInfo _ostree_fetch_service_method_info_fetch_summary =
{
  {
    -1,
    (gchar *) "fetch_summary",
    NULL,
    (GDBusArgInfo **) &_ostree_fetch_service_method_info_fetch_summary_OUT_ARG_pointers,
    NULL
  },
  "handle-fetch-summary",
  FALSE
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_fetch_summary_sig_OUT_ARG_result =
{
  {
    -1,
    (gchar *) "result",
    (gchar *) "ay",
    NULL
  },
  TRUE
};

static const _ExtendedGDBusArgInfo * const _ostree_fetch_service_method_info_fetch_summary_sig_OUT_ARG_pointers[] =
{
  &_ostree_fetch_service_method_info_fetch_summary_sig_OUT_ARG_result,
  NULL
};

static const _ExtendedGDBusMethodInfo _ostree_fetch_service_method_info_fetch_summary_sig =
{
  {
    -1,
    (gchar *) "fetch_summary_sig",
    NULL,
    (GDBusArgInfo **) &_ostree_fetch_service_method_info_fetch_summary_sig_OUT_ARG_pointers,
    NULL
  },
  "handle-fetch-summary-sig",
  FALSE
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_fetch_config_OUT_ARG_remote_mode =
{
  {
    -1,
    (gchar *) "remote_mode",
    (gchar *) "u",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_fetch_config_OUT_ARG_has_tombstone_comits =
{
  {
    -1,
    (gchar *) "has_tombstone_comits",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _ostree_fetch_service_method_info_fetch_config_OUT_ARG_pointers[] =
{
  &_ostree_fetch_service_method_info_fetch_config_OUT_ARG_remote_mode,
  &_ostree_fetch_service_method_info_fetch_config_OUT_ARG_has_tombstone_comits,
  NULL
};

static const _ExtendedGDBusMethodInfo _ostree_fetch_service_method_info_fetch_config =
{
  {
    -1,
    (gchar *) "fetch_config",
    NULL,
    (GDBusArgInfo **) &_ostree_fetch_service_method_info_fetch_config_OUT_ARG_pointers,
    NULL
  },
  "handle-fetch-config",
  FALSE
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_open_url_IN_ARG_base_uri =
{
  {
    -1,
    (gchar *) "base_uri",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _ostree_fetch_service_method_info_open_url_IN_ARG_pointers[] =
{
  &_ostree_fetch_service_method_info_open_url_IN_ARG_base_uri,
  NULL
};

static const _ExtendedGDBusMethodInfo _ostree_fetch_service_method_info_open_url =
{
  {
    -1,
    (gchar *) "open_url",
    (GDBusArgInfo **) &_ostree_fetch_service_method_info_open_url_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-open-url",
  FALSE
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_open_metalink_IN_ARG_metalink_uri =
{
  {
    -1,
    (gchar *) "metalink_uri",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _ostree_fetch_service_method_info_open_metalink_IN_ARG_pointers[] =
{
  &_ostree_fetch_service_method_info_open_metalink_IN_ARG_metalink_uri,
  NULL
};

static const _ExtendedGDBusMethodInfo _ostree_fetch_service_method_info_open_metalink =
{
  {
    -1,
    (gchar *) "open_metalink",
    (GDBusArgInfo **) &_ostree_fetch_service_method_info_open_metalink_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-open-metalink",
  FALSE
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_new_IN_ARG_tls_permissive =
{
  {
    -1,
    (gchar *) "tls_permissive",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_new_IN_ARG_tls_client_cert_path =
{
  {
    -1,
    (gchar *) "tls_client_cert_path",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_new_IN_ARG_tls_client_key_path =
{
  {
    -1,
    (gchar *) "tls_client_key_path",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_new_IN_ARG_tls_ca_path =
{
  {
    -1,
    (gchar *) "tls_ca_path",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_new_IN_ARG_http_proxy =
{
  {
    -1,
    (gchar *) "http_proxy",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _ostree_fetch_service_method_info_new_IN_ARG_pointers[] =
{
  &_ostree_fetch_service_method_info_new_IN_ARG_tls_permissive,
  &_ostree_fetch_service_method_info_new_IN_ARG_tls_client_cert_path,
  &_ostree_fetch_service_method_info_new_IN_ARG_tls_client_key_path,
  &_ostree_fetch_service_method_info_new_IN_ARG_tls_ca_path,
  &_ostree_fetch_service_method_info_new_IN_ARG_http_proxy,
  NULL
};

static const _ExtendedGDBusMethodInfo _ostree_fetch_service_method_info_new =
{
  {
    -1,
    (gchar *) "new",
    (GDBusArgInfo **) &_ostree_fetch_service_method_info_new_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-new",
  FALSE
};

static const _ExtendedGDBusArgInfo _ostree_fetch_service_method_info_progress_OUT_ARG_bytes_transferred =
{
  {
    -1,
    (gchar *) "bytes_transferred",
    (gchar *) "t",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _ostree_fetch_service_method_info_progress_OUT_ARG_pointers[] =
{
  &_ostree_fetch_service_method_info_progress_OUT_ARG_bytes_transferred,
  NULL
};

static const _ExtendedGDBusMethodInfo _ostree_fetch_service_method_info_progress =
{
  {
    -1,
    (gchar *) "progress",
    NULL,
    (GDBusArgInfo **) &_ostree_fetch_service_method_info_progress_OUT_ARG_pointers,
    NULL
  },
  "handle-progress",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _ostree_fetch_service_method_info_pointers[] =
{
  &_ostree_fetch_service_method_info_fetch_object,
  &_ostree_fetch_service_method_info_fetch_delta_part,
  &_ostree_fetch_service_method_info_fetch_delta_super,
  &_ostree_fetch_service_method_info_fetch_ref,
  &_ostree_fetch_service_method_info_fetch_summary,
  &_ostree_fetch_service_method_info_fetch_summary_sig,
  &_ostree_fetch_service_method_info_fetch_config,
  &_ostree_fetch_service_method_info_open_url,
  &_ostree_fetch_service_method_info_open_metalink,
  &_ostree_fetch_service_method_info_new,
  &_ostree_fetch_service_method_info_progress,
  NULL
};

static const GDBusAnnotationInfo _ostree_fetch_service_annotation_info_0 =
{
  -1,
  (gchar *) "org.freedesktop.DBus.GLib.CSymbol",
  (gchar *) "fetch_service",
  NULL
};

static const GDBusAnnotationInfo * const _ostree_fetch_service_annotation_info_pointers[] =
{
  &_ostree_fetch_service_annotation_info_0,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _ostree_fetch_service_interface_info =
{
  {
    -1,
    (gchar *) "ostree.fetch.service",
    (GDBusMethodInfo **) &_ostree_fetch_service_method_info_pointers,
    NULL,
    NULL,
    (GDBusAnnotationInfo **) &_ostree_fetch_service_annotation_info_pointers
  },
  "ostree-fetch-service",
};


/**
 * ostree_fetch_service_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-ostree-fetch-service.top_of_page">ostree.fetch.service</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
ostree_fetch_service_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_ostree_fetch_service_interface_info.parent_struct;
}

/**
 * ostree_fetch_service_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #OstreeFetchService interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
ostree_fetch_service_override_properties (GObjectClass *klass, guint property_id_begin)
{
  return property_id_begin - 1;
}



/**
 * OstreeFetchService:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-ostree-fetch-service.top_of_page">ostree.fetch.service</link>.
 */

/**
 * OstreeFetchServiceIface:
 * @parent_iface: The parent interface.
 * @handle_fetch_config: Handler for the #OstreeFetchService::handle-fetch-config signal.
 * @handle_fetch_delta_part: Handler for the #OstreeFetchService::handle-fetch-delta-part signal.
 * @handle_fetch_delta_super: Handler for the #OstreeFetchService::handle-fetch-delta-super signal.
 * @handle_fetch_object: Handler for the #OstreeFetchService::handle-fetch-object signal.
 * @handle_fetch_ref: Handler for the #OstreeFetchService::handle-fetch-ref signal.
 * @handle_fetch_summary: Handler for the #OstreeFetchService::handle-fetch-summary signal.
 * @handle_fetch_summary_sig: Handler for the #OstreeFetchService::handle-fetch-summary-sig signal.
 * @handle_new: Handler for the #OstreeFetchService::handle-new signal.
 * @handle_open_metalink: Handler for the #OstreeFetchService::handle-open-metalink signal.
 * @handle_open_url: Handler for the #OstreeFetchService::handle-open-url signal.
 * @handle_progress: Handler for the #OstreeFetchService::handle-progress signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-ostree-fetch-service.top_of_page">ostree.fetch.service</link>.
 */

typedef OstreeFetchServiceIface OstreeFetchServiceInterface;
G_DEFINE_INTERFACE (OstreeFetchService, ostree_fetch_service, G_TYPE_OBJECT);

static void
ostree_fetch_service_default_init (OstreeFetchServiceIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * OstreeFetchService::handle-fetch-object:
   * @object: A #OstreeFetchService.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_objtype: Argument passed by remote caller.
   * @arg_checksum: Argument passed by remote caller.
   * @arg_max_size: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-ostree-fetch-service.fetch_object">fetch_object()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call ostree_fetch_service_complete_fetch_object() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-fetch-object",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OstreeFetchServiceIface, handle_fetch_object),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    4,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_UINT, G_TYPE_STRING, G_TYPE_UINT64);

  /**
   * OstreeFetchService::handle-fetch-delta-part:
   * @object: A #OstreeFetchService.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_path: Argument passed by remote caller.
   * @arg_max_size: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-ostree-fetch-service.fetch_delta_part">fetch_delta_part()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call ostree_fetch_service_complete_fetch_delta_part() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-fetch-delta-part",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OstreeFetchServiceIface, handle_fetch_delta_part),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_UINT64);

  /**
   * OstreeFetchService::handle-fetch-delta-super:
   * @object: A #OstreeFetchService.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_from_revision: Argument passed by remote caller.
   * @arg_to_revision: Argument passed by remote caller.
   * @arg_branch: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-ostree-fetch-service.fetch_delta_super">fetch_delta_super()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call ostree_fetch_service_complete_fetch_delta_super() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-fetch-delta-super",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OstreeFetchServiceIface, handle_fetch_delta_super),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    4,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * OstreeFetchService::handle-fetch-ref:
   * @object: A #OstreeFetchService.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_name: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-ostree-fetch-service.fetch_ref">fetch_ref()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call ostree_fetch_service_complete_fetch_ref() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-fetch-ref",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OstreeFetchServiceIface, handle_fetch_ref),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * OstreeFetchService::handle-fetch-summary:
   * @object: A #OstreeFetchService.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-ostree-fetch-service.fetch_summary">fetch_summary()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call ostree_fetch_service_complete_fetch_summary() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-fetch-summary",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OstreeFetchServiceIface, handle_fetch_summary),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * OstreeFetchService::handle-fetch-summary-sig:
   * @object: A #OstreeFetchService.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-ostree-fetch-service.fetch_summary_sig">fetch_summary_sig()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call ostree_fetch_service_complete_fetch_summary_sig() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-fetch-summary-sig",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OstreeFetchServiceIface, handle_fetch_summary_sig),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * OstreeFetchService::handle-fetch-config:
   * @object: A #OstreeFetchService.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-ostree-fetch-service.fetch_config">fetch_config()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call ostree_fetch_service_complete_fetch_config() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-fetch-config",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OstreeFetchServiceIface, handle_fetch_config),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * OstreeFetchService::handle-open-url:
   * @object: A #OstreeFetchService.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_base_uri: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-ostree-fetch-service.open_url">open_url()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call ostree_fetch_service_complete_open_url() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-open-url",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OstreeFetchServiceIface, handle_open_url),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * OstreeFetchService::handle-open-metalink:
   * @object: A #OstreeFetchService.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_metalink_uri: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-ostree-fetch-service.open_metalink">open_metalink()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call ostree_fetch_service_complete_open_metalink() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-open-metalink",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OstreeFetchServiceIface, handle_open_metalink),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * OstreeFetchService::handle-new:
   * @object: A #OstreeFetchService.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_tls_permissive: Argument passed by remote caller.
   * @arg_tls_client_cert_path: Argument passed by remote caller.
   * @arg_tls_client_key_path: Argument passed by remote caller.
   * @arg_tls_ca_path: Argument passed by remote caller.
   * @arg_http_proxy: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-ostree-fetch-service.new">new()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call ostree_fetch_service_complete_new() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-new",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OstreeFetchServiceIface, handle_new),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    6,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_BOOLEAN, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * OstreeFetchService::handle-progress:
   * @object: A #OstreeFetchService.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-ostree-fetch-service.progress">progress()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call ostree_fetch_service_complete_progress() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-progress",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OstreeFetchServiceIface, handle_progress),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

}

/**
 * ostree_fetch_service_call_fetch_object:
 * @proxy: A #OstreeFetchServiceProxy.
 * @arg_objtype: Argument to pass with the method invocation.
 * @arg_checksum: Argument to pass with the method invocation.
 * @arg_max_size: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.fetch_object">fetch_object()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call ostree_fetch_service_call_fetch_object_finish() to get the result of the operation.
 *
 * See ostree_fetch_service_call_fetch_object_sync() for the synchronous, blocking version of this method.
 */
void
ostree_fetch_service_call_fetch_object (
    OstreeFetchService *proxy,
    guint arg_objtype,
    const gchar *arg_checksum,
    guint64 arg_max_size,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "fetch_object",
    g_variant_new ("(ust)",
                   arg_objtype,
                   arg_checksum,
                   arg_max_size),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * ostree_fetch_service_call_fetch_object_finish:
 * @proxy: A #OstreeFetchServiceProxy.
 * @out_temp_path: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to ostree_fetch_service_call_fetch_object().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with ostree_fetch_service_call_fetch_object().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_fetch_object_finish (
    OstreeFetchService *proxy,
    gchar **out_temp_path,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_temp_path);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_call_fetch_object_sync:
 * @proxy: A #OstreeFetchServiceProxy.
 * @arg_objtype: Argument to pass with the method invocation.
 * @arg_checksum: Argument to pass with the method invocation.
 * @arg_max_size: Argument to pass with the method invocation.
 * @out_temp_path: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.fetch_object">fetch_object()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See ostree_fetch_service_call_fetch_object() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_fetch_object_sync (
    OstreeFetchService *proxy,
    guint arg_objtype,
    const gchar *arg_checksum,
    guint64 arg_max_size,
    gchar **out_temp_path,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "fetch_object",
    g_variant_new ("(ust)",
                   arg_objtype,
                   arg_checksum,
                   arg_max_size),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_temp_path);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_call_fetch_delta_part:
 * @proxy: A #OstreeFetchServiceProxy.
 * @arg_path: Argument to pass with the method invocation.
 * @arg_max_size: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.fetch_delta_part">fetch_delta_part()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call ostree_fetch_service_call_fetch_delta_part_finish() to get the result of the operation.
 *
 * See ostree_fetch_service_call_fetch_delta_part_sync() for the synchronous, blocking version of this method.
 */
void
ostree_fetch_service_call_fetch_delta_part (
    OstreeFetchService *proxy,
    const gchar *arg_path,
    guint64 arg_max_size,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "fetch_delta_part",
    g_variant_new ("(st)",
                   arg_path,
                   arg_max_size),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * ostree_fetch_service_call_fetch_delta_part_finish:
 * @proxy: A #OstreeFetchServiceProxy.
 * @out_temp_path: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to ostree_fetch_service_call_fetch_delta_part().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with ostree_fetch_service_call_fetch_delta_part().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_fetch_delta_part_finish (
    OstreeFetchService *proxy,
    gchar **out_temp_path,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_temp_path);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_call_fetch_delta_part_sync:
 * @proxy: A #OstreeFetchServiceProxy.
 * @arg_path: Argument to pass with the method invocation.
 * @arg_max_size: Argument to pass with the method invocation.
 * @out_temp_path: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.fetch_delta_part">fetch_delta_part()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See ostree_fetch_service_call_fetch_delta_part() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_fetch_delta_part_sync (
    OstreeFetchService *proxy,
    const gchar *arg_path,
    guint64 arg_max_size,
    gchar **out_temp_path,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "fetch_delta_part",
    g_variant_new ("(st)",
                   arg_path,
                   arg_max_size),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_temp_path);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_call_fetch_delta_super:
 * @proxy: A #OstreeFetchServiceProxy.
 * @arg_from_revision: Argument to pass with the method invocation.
 * @arg_to_revision: Argument to pass with the method invocation.
 * @arg_branch: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.fetch_delta_super">fetch_delta_super()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call ostree_fetch_service_call_fetch_delta_super_finish() to get the result of the operation.
 *
 * See ostree_fetch_service_call_fetch_delta_super_sync() for the synchronous, blocking version of this method.
 */
void
ostree_fetch_service_call_fetch_delta_super (
    OstreeFetchService *proxy,
    const gchar *arg_from_revision,
    const gchar *arg_to_revision,
    const gchar *arg_branch,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "fetch_delta_super",
    g_variant_new ("(sss)",
                   arg_from_revision,
                   arg_to_revision,
                   arg_branch),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * ostree_fetch_service_call_fetch_delta_super_finish:
 * @proxy: A #OstreeFetchServiceProxy.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to ostree_fetch_service_call_fetch_delta_super().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with ostree_fetch_service_call_fetch_delta_super().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_fetch_delta_super_finish (
    OstreeFetchService *proxy,
    GVariant **out_result,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@ay)",
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_call_fetch_delta_super_sync:
 * @proxy: A #OstreeFetchServiceProxy.
 * @arg_from_revision: Argument to pass with the method invocation.
 * @arg_to_revision: Argument to pass with the method invocation.
 * @arg_branch: Argument to pass with the method invocation.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.fetch_delta_super">fetch_delta_super()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See ostree_fetch_service_call_fetch_delta_super() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_fetch_delta_super_sync (
    OstreeFetchService *proxy,
    const gchar *arg_from_revision,
    const gchar *arg_to_revision,
    const gchar *arg_branch,
    GVariant **out_result,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "fetch_delta_super",
    g_variant_new ("(sss)",
                   arg_from_revision,
                   arg_to_revision,
                   arg_branch),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@ay)",
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_call_fetch_ref:
 * @proxy: A #OstreeFetchServiceProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.fetch_ref">fetch_ref()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call ostree_fetch_service_call_fetch_ref_finish() to get the result of the operation.
 *
 * See ostree_fetch_service_call_fetch_ref_sync() for the synchronous, blocking version of this method.
 */
void
ostree_fetch_service_call_fetch_ref (
    OstreeFetchService *proxy,
    const gchar *arg_name,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "fetch_ref",
    g_variant_new ("(s)",
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * ostree_fetch_service_call_fetch_ref_finish:
 * @proxy: A #OstreeFetchServiceProxy.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to ostree_fetch_service_call_fetch_ref().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with ostree_fetch_service_call_fetch_ref().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_fetch_ref_finish (
    OstreeFetchService *proxy,
    gchar **out_result,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_call_fetch_ref_sync:
 * @proxy: A #OstreeFetchServiceProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.fetch_ref">fetch_ref()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See ostree_fetch_service_call_fetch_ref() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_fetch_ref_sync (
    OstreeFetchService *proxy,
    const gchar *arg_name,
    gchar **out_result,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "fetch_ref",
    g_variant_new ("(s)",
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_call_fetch_summary:
 * @proxy: A #OstreeFetchServiceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.fetch_summary">fetch_summary()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call ostree_fetch_service_call_fetch_summary_finish() to get the result of the operation.
 *
 * See ostree_fetch_service_call_fetch_summary_sync() for the synchronous, blocking version of this method.
 */
void
ostree_fetch_service_call_fetch_summary (
    OstreeFetchService *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "fetch_summary",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * ostree_fetch_service_call_fetch_summary_finish:
 * @proxy: A #OstreeFetchServiceProxy.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to ostree_fetch_service_call_fetch_summary().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with ostree_fetch_service_call_fetch_summary().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_fetch_summary_finish (
    OstreeFetchService *proxy,
    GVariant **out_result,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@ay)",
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_call_fetch_summary_sync:
 * @proxy: A #OstreeFetchServiceProxy.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.fetch_summary">fetch_summary()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See ostree_fetch_service_call_fetch_summary() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_fetch_summary_sync (
    OstreeFetchService *proxy,
    GVariant **out_result,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "fetch_summary",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@ay)",
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_call_fetch_summary_sig:
 * @proxy: A #OstreeFetchServiceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.fetch_summary_sig">fetch_summary_sig()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call ostree_fetch_service_call_fetch_summary_sig_finish() to get the result of the operation.
 *
 * See ostree_fetch_service_call_fetch_summary_sig_sync() for the synchronous, blocking version of this method.
 */
void
ostree_fetch_service_call_fetch_summary_sig (
    OstreeFetchService *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "fetch_summary_sig",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * ostree_fetch_service_call_fetch_summary_sig_finish:
 * @proxy: A #OstreeFetchServiceProxy.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to ostree_fetch_service_call_fetch_summary_sig().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with ostree_fetch_service_call_fetch_summary_sig().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_fetch_summary_sig_finish (
    OstreeFetchService *proxy,
    GVariant **out_result,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@ay)",
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_call_fetch_summary_sig_sync:
 * @proxy: A #OstreeFetchServiceProxy.
 * @out_result: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.fetch_summary_sig">fetch_summary_sig()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See ostree_fetch_service_call_fetch_summary_sig() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_fetch_summary_sig_sync (
    OstreeFetchService *proxy,
    GVariant **out_result,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "fetch_summary_sig",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@ay)",
                 out_result);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_call_fetch_config:
 * @proxy: A #OstreeFetchServiceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.fetch_config">fetch_config()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call ostree_fetch_service_call_fetch_config_finish() to get the result of the operation.
 *
 * See ostree_fetch_service_call_fetch_config_sync() for the synchronous, blocking version of this method.
 */
void
ostree_fetch_service_call_fetch_config (
    OstreeFetchService *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "fetch_config",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * ostree_fetch_service_call_fetch_config_finish:
 * @proxy: A #OstreeFetchServiceProxy.
 * @out_remote_mode: (out): Return location for return parameter or %NULL to ignore.
 * @out_has_tombstone_comits: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to ostree_fetch_service_call_fetch_config().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with ostree_fetch_service_call_fetch_config().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_fetch_config_finish (
    OstreeFetchService *proxy,
    guint *out_remote_mode,
    gboolean *out_has_tombstone_comits,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(ub)",
                 out_remote_mode,
                 out_has_tombstone_comits);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_call_fetch_config_sync:
 * @proxy: A #OstreeFetchServiceProxy.
 * @out_remote_mode: (out): Return location for return parameter or %NULL to ignore.
 * @out_has_tombstone_comits: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.fetch_config">fetch_config()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See ostree_fetch_service_call_fetch_config() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_fetch_config_sync (
    OstreeFetchService *proxy,
    guint *out_remote_mode,
    gboolean *out_has_tombstone_comits,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "fetch_config",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(ub)",
                 out_remote_mode,
                 out_has_tombstone_comits);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_call_open_url:
 * @proxy: A #OstreeFetchServiceProxy.
 * @arg_base_uri: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.open_url">open_url()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call ostree_fetch_service_call_open_url_finish() to get the result of the operation.
 *
 * See ostree_fetch_service_call_open_url_sync() for the synchronous, blocking version of this method.
 */
void
ostree_fetch_service_call_open_url (
    OstreeFetchService *proxy,
    const gchar *arg_base_uri,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "open_url",
    g_variant_new ("(s)",
                   arg_base_uri),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * ostree_fetch_service_call_open_url_finish:
 * @proxy: A #OstreeFetchServiceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to ostree_fetch_service_call_open_url().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with ostree_fetch_service_call_open_url().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_open_url_finish (
    OstreeFetchService *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_call_open_url_sync:
 * @proxy: A #OstreeFetchServiceProxy.
 * @arg_base_uri: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.open_url">open_url()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See ostree_fetch_service_call_open_url() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_open_url_sync (
    OstreeFetchService *proxy,
    const gchar *arg_base_uri,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "open_url",
    g_variant_new ("(s)",
                   arg_base_uri),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_call_open_metalink:
 * @proxy: A #OstreeFetchServiceProxy.
 * @arg_metalink_uri: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.open_metalink">open_metalink()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call ostree_fetch_service_call_open_metalink_finish() to get the result of the operation.
 *
 * See ostree_fetch_service_call_open_metalink_sync() for the synchronous, blocking version of this method.
 */
void
ostree_fetch_service_call_open_metalink (
    OstreeFetchService *proxy,
    const gchar *arg_metalink_uri,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "open_metalink",
    g_variant_new ("(s)",
                   arg_metalink_uri),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * ostree_fetch_service_call_open_metalink_finish:
 * @proxy: A #OstreeFetchServiceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to ostree_fetch_service_call_open_metalink().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with ostree_fetch_service_call_open_metalink().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_open_metalink_finish (
    OstreeFetchService *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_call_open_metalink_sync:
 * @proxy: A #OstreeFetchServiceProxy.
 * @arg_metalink_uri: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.open_metalink">open_metalink()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See ostree_fetch_service_call_open_metalink() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_open_metalink_sync (
    OstreeFetchService *proxy,
    const gchar *arg_metalink_uri,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "open_metalink",
    g_variant_new ("(s)",
                   arg_metalink_uri),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_call_new:
 * @proxy: A #OstreeFetchServiceProxy.
 * @arg_tls_permissive: Argument to pass with the method invocation.
 * @arg_tls_client_cert_path: Argument to pass with the method invocation.
 * @arg_tls_client_key_path: Argument to pass with the method invocation.
 * @arg_tls_ca_path: Argument to pass with the method invocation.
 * @arg_http_proxy: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.new">new()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call ostree_fetch_service_call_new_finish() to get the result of the operation.
 *
 * See ostree_fetch_service_call_new_sync() for the synchronous, blocking version of this method.
 */
void
ostree_fetch_service_call_new (
    OstreeFetchService *proxy,
    gboolean arg_tls_permissive,
    const gchar *arg_tls_client_cert_path,
    const gchar *arg_tls_client_key_path,
    const gchar *arg_tls_ca_path,
    const gchar *arg_http_proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "new",
    g_variant_new ("(bssss)",
                   arg_tls_permissive,
                   arg_tls_client_cert_path,
                   arg_tls_client_key_path,
                   arg_tls_ca_path,
                   arg_http_proxy),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * ostree_fetch_service_call_new_finish:
 * @proxy: A #OstreeFetchServiceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to ostree_fetch_service_call_new().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with ostree_fetch_service_call_new().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_new_finish (
    OstreeFetchService *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_call_new_sync:
 * @proxy: A #OstreeFetchServiceProxy.
 * @arg_tls_permissive: Argument to pass with the method invocation.
 * @arg_tls_client_cert_path: Argument to pass with the method invocation.
 * @arg_tls_client_key_path: Argument to pass with the method invocation.
 * @arg_tls_ca_path: Argument to pass with the method invocation.
 * @arg_http_proxy: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.new">new()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See ostree_fetch_service_call_new() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_new_sync (
    OstreeFetchService *proxy,
    gboolean arg_tls_permissive,
    const gchar *arg_tls_client_cert_path,
    const gchar *arg_tls_client_key_path,
    const gchar *arg_tls_ca_path,
    const gchar *arg_http_proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "new",
    g_variant_new ("(bssss)",
                   arg_tls_permissive,
                   arg_tls_client_cert_path,
                   arg_tls_client_key_path,
                   arg_tls_ca_path,
                   arg_http_proxy),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_call_progress:
 * @proxy: A #OstreeFetchServiceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.progress">progress()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call ostree_fetch_service_call_progress_finish() to get the result of the operation.
 *
 * See ostree_fetch_service_call_progress_sync() for the synchronous, blocking version of this method.
 */
void
ostree_fetch_service_call_progress (
    OstreeFetchService *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "progress",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * ostree_fetch_service_call_progress_finish:
 * @proxy: A #OstreeFetchServiceProxy.
 * @out_bytes_transferred: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to ostree_fetch_service_call_progress().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with ostree_fetch_service_call_progress().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_progress_finish (
    OstreeFetchService *proxy,
    guint64 *out_bytes_transferred,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(t)",
                 out_bytes_transferred);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_call_progress_sync:
 * @proxy: A #OstreeFetchServiceProxy.
 * @out_bytes_transferred: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-ostree-fetch-service.progress">progress()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See ostree_fetch_service_call_progress() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
ostree_fetch_service_call_progress_sync (
    OstreeFetchService *proxy,
    guint64 *out_bytes_transferred,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "progress",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(t)",
                 out_bytes_transferred);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * ostree_fetch_service_complete_fetch_object:
 * @object: A #OstreeFetchService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @temp_path: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-ostree-fetch-service.fetch_object">fetch_object()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
ostree_fetch_service_complete_fetch_object (
    OstreeFetchService *object,
    GDBusMethodInvocation *invocation,
    const gchar *temp_path)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(s)",
                   temp_path));
}

/**
 * ostree_fetch_service_complete_fetch_delta_part:
 * @object: A #OstreeFetchService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @temp_path: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-ostree-fetch-service.fetch_delta_part">fetch_delta_part()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
ostree_fetch_service_complete_fetch_delta_part (
    OstreeFetchService *object,
    GDBusMethodInvocation *invocation,
    const gchar *temp_path)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(s)",
                   temp_path));
}

/**
 * ostree_fetch_service_complete_fetch_delta_super:
 * @object: A #OstreeFetchService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @result: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-ostree-fetch-service.fetch_delta_super">fetch_delta_super()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
ostree_fetch_service_complete_fetch_delta_super (
    OstreeFetchService *object,
    GDBusMethodInvocation *invocation,
    GVariant *result)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@ay)",
                   result));
}

/**
 * ostree_fetch_service_complete_fetch_ref:
 * @object: A #OstreeFetchService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @result: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-ostree-fetch-service.fetch_ref">fetch_ref()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
ostree_fetch_service_complete_fetch_ref (
    OstreeFetchService *object,
    GDBusMethodInvocation *invocation,
    const gchar *result)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(s)",
                   result));
}

/**
 * ostree_fetch_service_complete_fetch_summary:
 * @object: A #OstreeFetchService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @result: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-ostree-fetch-service.fetch_summary">fetch_summary()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
ostree_fetch_service_complete_fetch_summary (
    OstreeFetchService *object,
    GDBusMethodInvocation *invocation,
    GVariant *result)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@ay)",
                   result));
}

/**
 * ostree_fetch_service_complete_fetch_summary_sig:
 * @object: A #OstreeFetchService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @result: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-ostree-fetch-service.fetch_summary_sig">fetch_summary_sig()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
ostree_fetch_service_complete_fetch_summary_sig (
    OstreeFetchService *object,
    GDBusMethodInvocation *invocation,
    GVariant *result)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@ay)",
                   result));
}

/**
 * ostree_fetch_service_complete_fetch_config:
 * @object: A #OstreeFetchService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @remote_mode: Parameter to return.
 * @has_tombstone_comits: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-ostree-fetch-service.fetch_config">fetch_config()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
ostree_fetch_service_complete_fetch_config (
    OstreeFetchService *object,
    GDBusMethodInvocation *invocation,
    guint remote_mode,
    gboolean has_tombstone_comits)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(ub)",
                   remote_mode,
                   has_tombstone_comits));
}

/**
 * ostree_fetch_service_complete_open_url:
 * @object: A #OstreeFetchService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-ostree-fetch-service.open_url">open_url()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
ostree_fetch_service_complete_open_url (
    OstreeFetchService *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * ostree_fetch_service_complete_open_metalink:
 * @object: A #OstreeFetchService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-ostree-fetch-service.open_metalink">open_metalink()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
ostree_fetch_service_complete_open_metalink (
    OstreeFetchService *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * ostree_fetch_service_complete_new:
 * @object: A #OstreeFetchService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-ostree-fetch-service.new">new()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
ostree_fetch_service_complete_new (
    OstreeFetchService *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * ostree_fetch_service_complete_progress:
 * @object: A #OstreeFetchService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @bytes_transferred: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-ostree-fetch-service.progress">progress()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
ostree_fetch_service_complete_progress (
    OstreeFetchService *object,
    GDBusMethodInvocation *invocation,
    guint64 bytes_transferred)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(t)",
                   bytes_transferred));
}

/* ------------------------------------------------------------------------ */

/**
 * OstreeFetchServiceProxy:
 *
 * The #OstreeFetchServiceProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * OstreeFetchServiceProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #OstreeFetchServiceProxy.
 */

struct _OstreeFetchServiceProxyPrivate
{
  GData *qdata;
};

static void ostree_fetch_service_proxy_iface_init (OstreeFetchServiceIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (OstreeFetchServiceProxy, ostree_fetch_service_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (OstreeFetchServiceProxy)
                         G_IMPLEMENT_INTERFACE (TYPE_OSTREE_FETCH_SERVICE, ostree_fetch_service_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (OstreeFetchServiceProxy, ostree_fetch_service_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_OSTREE_FETCH_SERVICE, ostree_fetch_service_proxy_iface_init));

#endif
static void
ostree_fetch_service_proxy_finalize (GObject *object)
{
  OstreeFetchServiceProxy *proxy = OSTREE_FETCH_SERVICE_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (ostree_fetch_service_proxy_parent_class)->finalize (object);
}

static void
ostree_fetch_service_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
ostree_fetch_service_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
ostree_fetch_service_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_ostree_fetch_service_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_OSTREE_FETCH_SERVICE);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_OSTREE_FETCH_SERVICE);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
ostree_fetch_service_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  OstreeFetchServiceProxy *proxy = OSTREE_FETCH_SERVICE_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_ostree_fetch_service_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_ostree_fetch_service_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
ostree_fetch_service_proxy_init (OstreeFetchServiceProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = ostree_fetch_service_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_OSTREE_FETCH_SERVICE_PROXY, OstreeFetchServiceProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), ostree_fetch_service_interface_info ());
}

static void
ostree_fetch_service_proxy_class_init (OstreeFetchServiceProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = ostree_fetch_service_proxy_finalize;
  gobject_class->get_property = ostree_fetch_service_proxy_get_property;
  gobject_class->set_property = ostree_fetch_service_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = ostree_fetch_service_proxy_g_signal;
  proxy_class->g_properties_changed = ostree_fetch_service_proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (OstreeFetchServiceProxyPrivate));
#endif
}

static void
ostree_fetch_service_proxy_iface_init (OstreeFetchServiceIface *iface)
{
}

/**
 * ostree_fetch_service_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-ostree-fetch-service.top_of_page">ostree.fetch.service</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call ostree_fetch_service_proxy_new_finish() to get the result of the operation.
 *
 * See ostree_fetch_service_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
ostree_fetch_service_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_OSTREE_FETCH_SERVICE_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "ostree.fetch.service", NULL);
}

/**
 * ostree_fetch_service_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to ostree_fetch_service_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with ostree_fetch_service_proxy_new().
 *
 * Returns: (transfer full) (type OstreeFetchServiceProxy): The constructed proxy object or %NULL if @error is set.
 */
OstreeFetchService *
ostree_fetch_service_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return OSTREE_FETCH_SERVICE (ret);
  else
    return NULL;
}

/**
 * ostree_fetch_service_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-ostree-fetch-service.top_of_page">ostree.fetch.service</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See ostree_fetch_service_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type OstreeFetchServiceProxy): The constructed proxy object or %NULL if @error is set.
 */
OstreeFetchService *
ostree_fetch_service_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_OSTREE_FETCH_SERVICE_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "ostree.fetch.service", NULL);
  if (ret != NULL)
    return OSTREE_FETCH_SERVICE (ret);
  else
    return NULL;
}


/**
 * ostree_fetch_service_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like ostree_fetch_service_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call ostree_fetch_service_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See ostree_fetch_service_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
ostree_fetch_service_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_OSTREE_FETCH_SERVICE_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "ostree.fetch.service", NULL);
}

/**
 * ostree_fetch_service_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to ostree_fetch_service_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with ostree_fetch_service_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type OstreeFetchServiceProxy): The constructed proxy object or %NULL if @error is set.
 */
OstreeFetchService *
ostree_fetch_service_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return OSTREE_FETCH_SERVICE (ret);
  else
    return NULL;
}

/**
 * ostree_fetch_service_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like ostree_fetch_service_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See ostree_fetch_service_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type OstreeFetchServiceProxy): The constructed proxy object or %NULL if @error is set.
 */
OstreeFetchService *
ostree_fetch_service_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_OSTREE_FETCH_SERVICE_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "ostree.fetch.service", NULL);
  if (ret != NULL)
    return OSTREE_FETCH_SERVICE (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * OstreeFetchServiceSkeleton:
 *
 * The #OstreeFetchServiceSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * OstreeFetchServiceSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #OstreeFetchServiceSkeleton.
 */

struct _OstreeFetchServiceSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_ostree_fetch_service_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  OstreeFetchServiceSkeleton *skeleton = OSTREE_FETCH_SERVICE_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_OSTREE_FETCH_SERVICE);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_OSTREE_FETCH_SERVICE);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_ostree_fetch_service_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  OstreeFetchServiceSkeleton *skeleton = OSTREE_FETCH_SERVICE_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_ostree_fetch_service_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_ostree_fetch_service_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  OstreeFetchServiceSkeleton *skeleton = OSTREE_FETCH_SERVICE_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_ostree_fetch_service_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _ostree_fetch_service_skeleton_vtable =
{
  _ostree_fetch_service_skeleton_handle_method_call,
  _ostree_fetch_service_skeleton_handle_get_property,
  _ostree_fetch_service_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
ostree_fetch_service_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return ostree_fetch_service_interface_info ();
}

static GDBusInterfaceVTable *
ostree_fetch_service_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_ostree_fetch_service_skeleton_vtable;
}

static GVariant *
ostree_fetch_service_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  OstreeFetchServiceSkeleton *skeleton = OSTREE_FETCH_SERVICE_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_ostree_fetch_service_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _ostree_fetch_service_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _ostree_fetch_service_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _ostree_fetch_service_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "ostree.fetch.service", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
ostree_fetch_service_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
}

static void ostree_fetch_service_skeleton_iface_init (OstreeFetchServiceIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (OstreeFetchServiceSkeleton, ostree_fetch_service_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (OstreeFetchServiceSkeleton)
                         G_IMPLEMENT_INTERFACE (TYPE_OSTREE_FETCH_SERVICE, ostree_fetch_service_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (OstreeFetchServiceSkeleton, ostree_fetch_service_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_OSTREE_FETCH_SERVICE, ostree_fetch_service_skeleton_iface_init));

#endif
static void
ostree_fetch_service_skeleton_finalize (GObject *object)
{
  OstreeFetchServiceSkeleton *skeleton = OSTREE_FETCH_SERVICE_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (ostree_fetch_service_skeleton_parent_class)->finalize (object);
}

static void
ostree_fetch_service_skeleton_init (OstreeFetchServiceSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = ostree_fetch_service_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_OSTREE_FETCH_SERVICE_SKELETON, OstreeFetchServiceSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
ostree_fetch_service_skeleton_class_init (OstreeFetchServiceSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = ostree_fetch_service_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = ostree_fetch_service_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = ostree_fetch_service_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = ostree_fetch_service_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = ostree_fetch_service_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (OstreeFetchServiceSkeletonPrivate));
#endif
}

static void
ostree_fetch_service_skeleton_iface_init (OstreeFetchServiceIface *iface)
{
}

/**
 * ostree_fetch_service_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-ostree-fetch-service.top_of_page">ostree.fetch.service</link>.
 *
 * Returns: (transfer full) (type OstreeFetchServiceSkeleton): The skeleton object.
 */
OstreeFetchService *
ostree_fetch_service_skeleton_new (void)
{
  return OSTREE_FETCH_SERVICE (g_object_new (TYPE_OSTREE_FETCH_SERVICE_SKELETON, NULL));
}

